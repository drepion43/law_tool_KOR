startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
    description: No configuration required.
  commandFunction: 
    |-
    (config) => ({
      command: "python3",
      args: ["./src/server.py"],
      env: {},
    })